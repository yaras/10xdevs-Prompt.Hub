@page "/"
@rendermode InteractiveServer

@inject NavigationManager Navigation

@code
{
    protected override void OnInitialized()
        => this.Navigation.NavigateTo("/my-prompts", replace: true);
}
